kind: pipeline
name: default

workspace:
  base: /go
  path: /packa

steps:
  - name: test
    image: golang:1.12
    commands:
      - go get
      - go test ./... -v -cover

  - name: build
    image: golang
    commands:
      - go get
      - go build
