package constants

const (
	// ConfigFileLocalDir is the user-local directory to search for
	// a config file
	ConfigFileLocalDir = "$HOME/.packago"
	// ConfigFileName is the default name for the config file
	ConfigFileName = "packago"
)
